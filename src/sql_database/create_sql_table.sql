CREATE TABLE blacklist (
    user_id       varchar(255),
    order_number  varchar(255),
    cell          varchar(255),
    ipv4          varchar(255),
    en0           varchar(255),
    credit_card   varchar(255)
);
